# Fort of Praise Homepage

A premium, responsive, CMS-ready homepage concept for the Fort of Praise Christian ministry. The static prototype uses editable data structures in `script.js` and matching Sanity schema definitions in `sanity/schemas`.

## Run locally

```bash
python3 -m http.server 4173
```

Open <http://localhost:4173>.
